spring:
  datasource:
    url: jdbc:h2:mem:campusloop;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
  mail:
    host: localhost
    port: 2525
    username: test
    password: test

app:
  jwt:
    secret: test-secret-key-that-is-long-enough-for-jwt-signing-12345
    expiration-ms: 86400000

cloudinary:
  cloud-name: test-cloud
  api-key: test-key
  api-secret: test-secret
